package LocationServices;

import android.location.LocationRequest;
import android.Manifest;
import com.google.android.gms.common.api.GoogleApiClient;

public class FusedLocationApi {
    public static void requestLocationUpdates(GoogleApiClient googleApiClient, LocationRequest locationRequest, navmaps navmaps) {
    }

    public static void removeLocationUpdates(GoogleApiClient googleApiClient, navmaps navmaps) {
    }
}
